import type { FieldsetDefinition } from 'sanity'

export const baseDocumentFieldset: FieldsetDefinition = {
  name: 'baseDocument',
  title: 'Base Document',
  options: {
    collapsible: true,
    collapsed: false,
  },
}

