import type { FieldsetDefinition } from 'sanity'

export const seoFieldset: FieldsetDefinition = {
  name: 'seo',
  title: 'SEO & Metadata',
  options: {
    collapsible: true,
    collapsed: true,
  },
}

