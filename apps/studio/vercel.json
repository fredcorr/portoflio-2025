{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "if [ -n \"$VERCEL_GIT_PREVIOUS_SHA\" ] && npx turbo query affected --base=\"$VERCEL_GIT_PREVIOUS_SHA\" --packages studio --exit-code; then exit 0; else exit 1; fi"
}
