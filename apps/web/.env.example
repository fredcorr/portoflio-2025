# ===========================================
# WEB APP ENVIRONMENT VARIABLES
# ===========================================
# This file is OPTIONAL - Next.js automatically uses root .env variables
# Client-side variables (NEXT_PUBLIC_*) are mapped in next.config.ts
#
# Only create this file if you need to:
# - Override root values for local development  
# - Add web-app specific environment variables

# Example app-specific variables:
# NEXT_PUBLIC_SITE_URL=http://localhost:3000
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Example overrides (will take precedence over root .env):
# SANITY_PROJECT_ID=different-project-id
# SANITY_DATASET=development
